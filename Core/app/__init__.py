"""Core application package.

This package contains the FastAPI backend for the local VTuber-like assistant.
If you are new to Python, you can think of this folder as the "server source code"
root, and each subfolder as a functional module (api, agent, services, storage).
"""

# Core 应用包入口：统一承载后端各模块。
